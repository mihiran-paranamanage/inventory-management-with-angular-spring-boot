import { LkrCurrencyPipe } from './lkr-currency.pipe';

describe('LkrCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new LkrCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
